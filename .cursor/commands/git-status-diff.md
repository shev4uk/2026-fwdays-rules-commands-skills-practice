# Check Git Status and Diff

Quickly inspect what changed before committing.

## Prompt

Run `git status` and `git diff` in the repo root and summarize the changes.

## Checklist

- Note untracked files
- Summarize key changes in modified files
